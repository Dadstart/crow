<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Dadstart.Labs.Crow.App.Pages.RemindersPage"
             x:DataType="viewModels:RemindersPageViewModel"
             xmlns:viewModels="clr-namespace:Dadstart.Labs.Crow.App.ViewModels"
             xmlns:models="clr-namespace:Dadstart.Labs.Crow.Models;assembly=Crow.Models"
             Title="Reminders">
    <Grid RowDefinitions="Auto,*" Padding="10">
        <VerticalStackLayout Grid.Row="0" Spacing="10" Padding="10">
            <Entry Text="{Binding Title}" Placeholder="Title" />
            <Editor Text="{Binding Description}" Placeholder="Description (optional)" HeightRequest="60" />
            <DatePicker Date="{Binding DueDate}" />
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="Create"
                        Command="{Binding CreateReminderCommand}" />
                <Button Grid.Column="1" 
                        Text="Update"
                        Command="{Binding UpdateReminderCommand}"
                        CommandParameter="{Binding SelectedReminder}" />
            </Grid>
            <Button Text="Clear Selection" 
                    Command="{Binding ClearSelectionCommand}" />
        </VerticalStackLayout>
        
        <RefreshView Grid.Row="1" 
                     IsRefreshing="{Binding IsLoading}"
                     Command="{Binding LoadRemindersCommand}">
            <CollectionView ItemsSource="{Binding Reminders}" SelectionMode="Single" SelectionChanged="OnReminderSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Reminder">
                        <Grid Padding="10" ColumnDefinitions="*,Auto,Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Title}" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" LineBreakMode="TailTruncation" />
                                <Label Text="{Binding DueDate, StringFormat='Due: {0:g}'}" 
                                       FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                            <CheckBox Grid.Column="1" 
                                      IsChecked="{Binding IsCompleted}"
                                      CheckedChanged="OnReminderToggled" />
                            <Button Grid.Column="2" 
                                    Text="Delete"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:RemindersPageViewModel}}, Path=DeleteReminderCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
