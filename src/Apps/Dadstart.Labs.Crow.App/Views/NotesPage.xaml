<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Dadstart.Labs.Crow.Views.NotesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:Dadstart.Labs.Crow.ViewModels"
    xmlns:models="clr-namespace:Dadstart.Labs.Crow.Models;assembly=Dadstart.Labs.Crow.Core"
    x:DataType="viewmodels:NotesViewModel"
    Title="Notes">

    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Add"
            Command="{Binding AddQuickNoteCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*">
        <SearchBar
            Placeholder="Search"
            Text="{Binding SearchQuery}"
            SearchCommand="{Binding RefreshCommand}" />

        <RefreshView
            Grid.Row="1"
            Command="{Binding RefreshCommand}"
            IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Notes}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:SecureNote">
                        <Border
                            Padding="12"
                            Margin="8"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 12">
                            <VerticalStackLayout>
                                <Label
                                    Text="{Binding Title}"
                                    FontAttributes="Bold"
                                    FontSize="18" />
                                <Label
                                    Text="{Binding RichTextBody}"
                                    LineBreakMode="TailTruncation" />
                                <Label
                                    Text="{Binding UpdatedAt, StringFormat='Updated {0:g}'}"
                                    FontSize="12"
                                    TextColor="Gray" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>

